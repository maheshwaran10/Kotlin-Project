<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.companyproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CompanyProject">
        <activity
            android:name=".MvcPatternActivity"
            android:exported="true" />
        <activity
            android:name=".lottie.LottieAnimationActivity"
            android:exported="true" />

        <activity
            android:name=".CoroutineExampleActivity"
            android:exported="true" />
        <activity
            android:name=".navigateGraph.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>